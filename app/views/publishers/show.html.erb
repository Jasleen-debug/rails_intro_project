<h2 class="text-center p-4"><%= @publisher.name %></h2>
<p class="text-center p-4">
  This publisher published <%= @publisher.books.count %> of the top books in the GoodReads eLibrary.
</p>
<div class="col p-4">
<div class="p-3 border bg-light">
<ul class="list-group">
  <% @publisher.books.each do |book| %>
    <li class="list-group-item list-group-item-info"><%= link_to book.title, book %> with a listing of <%= book.average_listing %> / 10</li>
  <% end %>
</ul>
</div>
</div>